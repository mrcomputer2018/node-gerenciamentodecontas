export default class GerenciamentoDeContas {

    static menu(): void {
        console.log("\n============ Menu ============")
    }

    static main(): void {
        console.log("\n============ Bem-vindo ao Banco ============")
        this.menu();
    }
}